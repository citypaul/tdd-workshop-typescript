// Loaded by Vitest Browser Mode (see vitest.config.ts) so every component
// test renders with the same Tailwind theme and base styles the running
// app uses. Without this, test iframes render unstyled.
import '../web/styles/global.css';
